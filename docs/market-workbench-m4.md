# Phase 3A-M M4 独立验收记录：全市场行情工作台

## 验收结论

M1-M4 的“低频公开行情研究工作台”范围已通过。该结论不等同于交易所级实时终端，也不改变 Phase 3 的 `can_submit_live=false`。

## 独立运行态检查

- 浏览器访问 `http://127.0.0.1:5173/market`，市场总览展示六个指数；公共源无响应时市场宽度明确显示“暂不可用”，不把缺失指标当成 0。
- 全市场证券主数据显示 5,556 条，分页从第 1 页切换到第 2 页后代码、名称和当前价格均同步变化；当前页行情源显示 `tencent:qt`。
- 搜索、交易所/板块筛选和分页保留在服务器端；列表只请求当前页 50 个代码，避免一次请求整个 A 股快照。
- 证券详情在同一证券上完成日 K、周 K、月 K 切换；周/月返回真实交易日周期，详情标题同步显示周期和数据截止日。
- 同时打开均线、布林带、MACD、RSI、KDJ 后，价格图、成交量和三个指标子图均渲染；tooltip 显示 OHLC、成交量和已启用指标；空值留白。
- 刷新按钮和 30 秒定时刷新同时更新自选、指数、市场宽度和当前页行情；切换证券的旧请求不会覆盖新选择。
- 周期、指标开关和“证券+周期”缩放范围在浏览器刷新后恢复；存储不可用时回退到内存状态。

## 自动化与接口验证

- 全量后端回归：`274 passed, 1 deselected, 4 warnings`；排除项为既有会等待外部股票池的用例。
- 前端 Node 单测：`6 passed`；`vue-tsc` 和 Vite 生产构建通过（678 modules）。
- 运行时接口：日 K 242 条、周 K 128 条、月 K 31 条；Tencent 回退返回 `adjust_applied=qfq` 与 `ADJUSTMENT_FALLBACK`；相同请求第二次命中 `cache=memory`。
- `/market/quotes` 的 205 代码请求拆成 `100/100/5`，超过 500 个代码返回 422；单批失败可返回 `partial`，全批失败返回结构化 503。

## 未纳入本阶段的明确边界

- 当前没有真实券商连接、账户授权、下单、撤单或真实资金操作；所有真实交易能力仍由后续 Phase 3B/3C 控制面阻断。
- 公共 Tencent/AKShare 源无 SLA；非交易日行情会标记过期，源不可用会显示明确错误。
- 尚未加入浏览器自动化测试套件；本记录的浏览器部分为独立人工运行态验收。后续如接入 Playwright，再将快速翻页、断网恢复和 ECharts DOM 断言纳入 CI。

# 前端快速迭代记录

## 2026-09-08 · 深度研究净值图与正式报告

- 回测页按实验名称展示历史，默认选择全段低波基线；点击研究实验可定位三线图。三条曲线来自已绑定哈希的comparison.json，缺值断线，包含缩放、tooltip、最大上涨/最大回撤色块与区间卡。
- PNG导出保留标题、起止日期、两类区间日期/幅度、指数不含分红与研究限制说明；已通过真实下载文件检查（约257KB），不是只验证点击按钮。
- 研究数据页单独展示冻结v1/v2清单，避免把旧日线缓存13只与独立500股研究档案混淆。
- 正式静态报告包含36张真实图、三候选概览和CSV/审计；按用户示例统一红策略、蓝上证、绿沪深300，绿色上涨/紫色回撤背景。已目检三个全段基线和高回撤压力图，属于代表性抽检。
- 新图表完成桌面与PNG实测。窄屏代码已适配，但本轮浏览器尺寸覆盖未作用于目标页，未把新图表移动端记作实测通过。
- 前端10项测试与类型/生产构建通过；charts-core约558KB的非阻断提示仍存在。

## 2026-09-08 · 研究数据中心与回测工作流

新增 `/data`：真实日线库存、10 条分页、代码筛选、证券选取、区间覆盖预检、缺失日期展开、旧报告过期提示、JSON 导出、基金归档及已接入数据源能力。库存只说明文件实际范围，预检通过不冒充公司行动和全部策略需求已验证。

回测页新增实验摘要、研究数据入口、执行状态刷新、停止入口和错误详情；未知收益保留为空，零回撤正确显示。导航增加研究数据，将账户入口文案统一为“账户接入”，去掉旧阶段号及静态连接绿灯。日期校验改为 UTC 日历校验，修正上海时区误判；320px 日期表单改为纵向排列。

验证：前端 10 项测试、类型检查和构建通过。浏览器实测库存 13 只，代码筛选、第二页3条、选取证券预检、完整/缺失结果、修改条件后报告过期、回测日期传递、来源未知态、基金归档空态；320/390px 及桌面无页面横向溢出。JSON 导出使用原生持久 Blob 下载链接；浏览器下载事件监听超时，但已在本机 Downloads 确認新导出的 `600519.SH`、2024-01-02—2024-01-31 报告（22 个交易日），并复核 JSON 内容及数据哈希。导出校验同时修正 HTTP 附加 meta 不应参与覆盖证据哈希复算的问题。构建保留既有 ECharts 大包提示。

## 2026-09-07 · 美股主要指数工作台

1. `CrossMarketPanel.vue` 新增“主要指数”区块，展示纳斯达克 100（`^NDX`）、道琼斯工业指数（`^DJI`）和标普 500（`^GSPC`）。
2. 指数区块使用独立的加载、错误和来源状态，不与自选股票列表混淆；指数卡点击后进入现有 `/market/detail` 详情页。
3. 美股刷新流程同时刷新自选标的、主要指数和当前标的历史行情；基金市场不发起美股指数请求。
4. `YahooUSMarketDataProvider` 放宽符号校验以支持 Yahoo 指数前缀 `^`，并将上述三类标的标记为 `asset_type=index`，保留 `yahoo:chart` 来源和时效字段。
5. `MarketDetail.vue` 将美股指数显示为“指数行情”，并在报价加载前后均阻止进入模拟下单。
6. 新增指数卡桌面、平板和窄屏布局规则，保持与自选行情卡一致的来源、价格、涨跌幅和时效展示。

统一核验结果：Yahoo 指数报价接口 200；浏览器展示 3 张指数卡，点击 `^NDX` 可进入 `/market/detail` 并显示“指数行情”；后端目标测试 44 passed，前端测试 6 passed，生产构建成功，浏览器错误数为 0。研究/模拟边界不变，不连接真实券商。

## 2026-09-07 · 三类市场统一信息层级

1. A 股市场页将“市场总览”和“当日热门”并列为首屏主区块；热门区分活跃度与涨幅排行，并直接复用全市场返回的成交额/成交量/涨跌幅字段。
2. 基金和美股工作台新增“市场总览”摘要，明确当前观察池数量、上涨数、下跌数和研究状态。
3. 基金/美股工作台新增“当日热门”与“涨幅排行”；由于跨市场接口当前只返回观察池，界面明确标注“观察池”并不伪称全市场排名。
4. 三类市场的搜索/筛选区保持为独立的第三层入口；所有排行行和指数卡均可跳转现有详情页。
5. 后端 A 股市场总览新增 `top_active` 与 `top_gainers`，仅基于源返回字段排序；未提供成交量/成交额的市场不生成虚假活跃度数据。

统一核验结果：A 股总览返回 5,558 条样本并展示活跃度/涨幅各 5 条；基金与美股均展示市场总览、观察池热门和涨幅排行；美股指数卡展示 `^NDX`、`^DJI`、`^GSPC`，点击可进入指数详情。基金 `110022` 历史 K 线在公开源无数据时按既有 fail-closed 规则返回 503 并在界面显示“历史行情暂不可用”，不伪造数据。后端目标测试 44 passed，前端测试 6 passed，生产构建成功；指数详情浏览器错误数为 0。研究/模拟边界不变，不连接真实券商。

## 2026-09-07 · 详情页信息密度收敛

删除详情页“所有字段保留来源与接收时点。”说明，仅保留“行情摘要”标题；来源、接收时间和时效仍在摘要字段中直接展示。生产构建通过。

## 2026-09-07 · 价格走势说明收敛

删除价格走势区域“支持日 K、周 K、月 K；指标仅基于当前返回的历史数据计算。”说明，仅保留标的标题、周期切换和指标按钮，图表数据源与时效仍保留。

## 2026-09-07 · 详情页表现与走势图增强

1. 详情页统一增加“涨跌幅”表现条，展示当日涨跌幅、所选区间收益、区间高点和区间低点。
2. 价格走势新增近 1 月、3 月、6 月、1 年、3 年快捷范围，切换后按真实历史数据重新请求。
3. 新增“K线 / 走势”视图切换；走势视图以区间起点为基准显示累计收益率，日 K、周 K、月 K 和原有指标开关继续保留。
4. A 股、国内基金、美股详情页共用同一套组件；无历史数据时继续显示不可用状态，不生成虚假表现数据。

定向核验结果：详情页可见表现条、5 个时间范围按钮和 K线/走势切换；切换至近 3 月与走势视图后图表和区间收益正常更新；`npm run build` 通过，浏览器错误数为 0。

## 2026-09-07 · 自选快速追踪

将 A 股市场页底部“行情快照明细”替换为“自选快速追踪”卡片区：展示自选标的最新价、涨跌幅、成交量、成交额、数据时效和详情入口；保留失败重试与研究数据边界，移除无操作价值的宽表结构。

## 2026-09-07 · 详情页边界卡片移除

删除详情页“使用边界”整块说明卡片，摘要区调整为单列满宽；报价摘要内的最新价、涨跌幅、成交量、成交额、源时间、数据源和时效字段继续保留。

## 2026-09-07 · 账户接入页信息重构

1. “实盘准备”页改为“账户接入”，顶部直接说明账户余额、持仓和资产状态的用途。
2. 删除“订单草案预检”及“订单草案记录”表单，避免用户误解为真实下单入口。
3. 新增“账户概览”卡片，展示现有模拟账户的总权益、可用现金、持仓市值和持仓数量，并提供进入模拟账户的入口。
4. 券商连接登记保留为账户接入准备；操作员令牌收纳至高级设置，安全阻断状态继续明确显示。

## 2026-09-07 · 黄金行情工作台

1. 市场行情新增“黄金”分页，沿用现有暗色主题、总览、热门排行、关注列表和详情链路。
2. 黄金默认追踪沪金主连（`AU0`）、国际现货黄金（`XAU`）、COMEX 黄金期货（`GC=F`）、SPDR 黄金 ETF（`GLD`）和 iShares 黄金 ETF（`IAU`）。
3. 新增 `GoldMarketDataProvider`：国内金价/国际现货使用新浪公开行情，COMEX 与 ETF 使用 Yahoo Chart；每条报价保留来源、币种、时效和研究模式标记。
4. 黄金标的详情支持现有涨跌幅表现条、K线/走势切换和日/周/月周期；黄金及商品标的不会出现模拟下单入口。

定向核验结果：黄金报价接口返回 5 个标的，来源包含 `sina:gold` 与 `yahoo:chart`；黄金分页和详情路由已接入。新浪 `AU0` 历史接口当前仅返回至 2024-07-17，超出范围时按既有 fail-closed 规则显示历史数据不可用，不填充虚假历史行情。
黄金工作台默认选中 `GC=F` 以确保进入页面即可看到 Yahoo 历史走势图；`AU0` 仍提供国内实时金价卡片，点击后若历史源无覆盖会明确显示不可用状态。

## 2026-09-07 · 绩效分析并入回测中心

1. 新增 `PerformanceAnalysisPanel.vue`，将已完成回测选择、绩效指标、α/β 归因和多回测对比作为回测中心内的连续工作流组件。
2. `BacktestRunner.vue` 直接传入当前回测记录，绩效面板复用同一份回测历史，避免重复加载和页面跳转。
3. 移除独立“绩效”导航入口；旧 `/analytics` 路径保留兼容重定向，历史书签会回到 `/backtest`。
4. 面板文案收敛为操作性提示，保留指标、数据证据和研究边界，不再单独占用一个页面。

## 2026-09-07 · 策略协议工作台

1. `Strategy` 基类新增机器可读的 `kk-quant-strategy-v1` 协议定义，统一生命周期、输入、目标权重输出和参数约束。
2. 新增 `GET /api/v1/strategies/protocol`，策略管理页从服务端加载协议版本，避免页面说明与引擎契约分离。
3. 策略管理页新增协议流程卡、市场/输出/执行约束摘要、按协议创建表单和 JSON 参数模板；策略卡片明确执行类路径与参数数量。
4. 新增 `docs/strategy-protocol-v1.md`，提供最小可运行策略示例和后续策略开发规则。
5. 策略创建与更新接口校验本地 `strategies.<module>.<ClassName>` 类路径，避免登记无法进入统一引擎的外部执行入口。

定向核验结果：策略页展示协议 1.0、六步生命周期和 4 个已登记策略；协议接口返回 200；页面控制台错误数为 0。未执行全量测试。
